apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "ai-cloud-operator.fullname" . }}-workload-editor-role
  labels:
  {{- include "ai-cloud-operator.labels" . | nindent 4 }}
rules:
- apiGroups:
  - apps.aicloud.dev
  resources:
  - workloads
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - apps.aicloud.dev
  resources:
  - workloads/status
  verbs:
  - get
